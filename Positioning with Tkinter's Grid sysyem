from tkinter import *

root=Tk()

#creating a Label Widget
myLabel = Label(root, text="Hello World!")
myLabel2= Label(root, text="Divyaaaaaaaaaaaa")

myLabel.grid(row=0, column=0)
myLabel2.grid(row=0, column=1)

root.mainloop()
